# Public API base URL used by the browser to reach the Rune backend.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Public WebSocket base URL. Usually the same host as NEXT_PUBLIC_API_URL
# but with the ws:// or wss:// scheme.
NEXT_PUBLIC_WS_URL=ws://localhost:8080
